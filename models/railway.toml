[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -w 1 -b 0.0.0.0:$PORT --timeout 120 --preload inference_server:app"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "inference-server"
